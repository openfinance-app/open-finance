<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <!-- Least-verbose config for plain unit tests (no Spring context).
         SpringBootTest slices use logback-spring.xml with the 'test' profile instead;
         this file is only picked up by logback-classic for non-Spring tests. -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <logger name="org.openfinance" level="ERROR"/>
    <logger name="org.openfinance.util.LoggingAspect" level="OFF"/>
    <logger name="org.openfinance.config.PerformanceMonitoringAspect" level="OFF"/>
    <logger name="org.springframework" level="WARN"/>
    <logger name="org.hibernate" level="WARN"/>
    <logger name="com.zaxxer.hikari" level="WARN"/>
    <logger name="org.flywaydb" level="WARN"/>
    <logger name="io.jsonwebtoken" level="WARN"/>

    <root level="WARN">
        <appender-ref ref="CONSOLE"/>
    </root>
</configuration>
