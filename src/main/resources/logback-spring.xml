<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <!-- Silence logback's own status INFO at startup (keeps test output minimal) -->
    <statusListener class="ch.qos.logback.core.status.NopStatusListener" />

    <!-- Requirement REQ-3.3: Comprehensive logging configuration -->
    <include resource="org/springframework/boot/logging/logback/defaults.xml" />

    <!-- Properties -->
    <springProperty scope="context" name="APP_NAME" source="spring.application.name" defaultValue="open-finance"/>
    <property name="LOG_PATH" value="${LOG_PATH:-./logs}"/>
    <property name="LOG_FILE" value="${LOG_FILE:-${LOG_PATH}/application.log}"/>
    <property name="LOG_PATTERN" value="%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n"/>

    <!-- Console Appender (with color support via Spring Boot) -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder class="ch.qos.logback.classic.encoder.PatternLayoutEncoder">
            <pattern>%clr(%d{yyyy-MM-dd HH:mm:ss.SSS}){faint} %clr([%thread]){magenta} %clr(%-5level){highlight} %clr(%logger{36}){cyan} %clr(-){faint} %msg%n</pattern>
            <charset>UTF-8</charset>
        </encoder>
    </appender>
    
    <!-- Console Appender for test (no color) -->
    <appender name="CONSOLE_TEST" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>${LOG_PATTERN}</pattern>
            <charset>UTF-8</charset>
        </encoder>
    </appender>

    <!-- File Appender -->
    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_FILE}</file>
        <encoder>
            <pattern>${LOG_PATTERN}</pattern>
            <charset>UTF-8</charset>
        </encoder>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <!-- Daily rollover -->
            <fileNamePattern>${LOG_PATH}/application.%d{yyyy-MM-dd}.%i.log</fileNamePattern>
            <!-- Max file size -->
            <maxFileSize>10MB</maxFileSize>
            <!-- Keep 30 days of history -->
            <maxHistory>30</maxHistory>
            <!-- Total size cap -->
            <totalSizeCap>1GB</totalSizeCap>
        </rollingPolicy>
    </appender>

    <!-- Error File Appender -->
    <appender name="ERROR_FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_PATH}/error.log</file>
        <filter class="ch.qos.logback.classic.filter.ThresholdFilter">
            <level>ERROR</level>
        </filter>
        <encoder>
            <pattern>${LOG_PATTERN}</pattern>
            <charset>UTF-8</charset>
        </encoder>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>${LOG_PATH}/error.%d{yyyy-MM-dd}.%i.log</fileNamePattern>
            <maxFileSize>10MB</maxFileSize>
            <maxHistory>90</maxHistory>
            <totalSizeCap>500MB</totalSizeCap>
        </rollingPolicy>
    </appender>

    <!-- Async Appenders for better performance -->
    <appender name="ASYNC_FILE" class="ch.qos.logback.classic.AsyncAppender">
        <appender-ref ref="FILE"/>
        <queueSize>512</queueSize>
        <discardingThreshold>0</discardingThreshold>
    </appender>

    <appender name="ASYNC_ERROR_FILE" class="ch.qos.logback.classic.AsyncAppender">
        <appender-ref ref="ERROR_FILE"/>
        <queueSize>512</queueSize>
        <discardingThreshold>0</discardingThreshold>
    </appender>

    <!-- Spring Boot specific loggers (default — overridden per profile below) -->
    <logger name="org.springframework" level="INFO"/>
    <logger name="org.springframework.web" level="INFO"/>
    <logger name="org.springframework.security" level="INFO"/>
    <logger name="org.springframework.boot" level="INFO"/>
    
    <!-- Hibernate/JPA loggers -->
    <logger name="org.hibernate" level="WARN"/>
    <logger name="org.hibernate.SQL" level="WARN"/>
    <logger name="org.hibernate.type.descriptor.sql.BasicBinder" level="WARN"/>
    
    <!-- Application loggers (default WARN keeps unit-test output minimal; dev/local override to DEBUG) -->
    <logger name="org.openfinance" level="WARN"/>
    
    <!-- Security sensitive - do not log at DEBUG in production -->
    <logger name="org.openfinance.security" level="INFO"/>
    
    <!-- Third-party libraries -->
    <logger name="com.zaxxer.hikari" level="WARN"/>
    <logger name="io.jsonwebtoken" level="WARN"/>
    <logger name="org.flywaydb" level="WARN"/>

    <!-- Root loggers — one per profile to avoid duplicate console output.
         The previous top-level root + per-profile roots caused every log line to be
         emitted twice when a profile was active (test, dev, prod). Now the default
         root is only active when no explicit profile matches. -->
    <springProfile name="default">
        <root level="INFO">
            <appender-ref ref="CONSOLE"/>
            <appender-ref ref="ASYNC_FILE"/>
            <appender-ref ref="ASYNC_ERROR_FILE"/>
        </root>
    </springProfile>

    <springProfile name="dev,local">
        <logger name="org.openfinance" level="DEBUG"/>
        <logger name="com.zaxxer.hikari" level="INFO"/>
        <root level="INFO">
            <appender-ref ref="CONSOLE"/>
            <appender-ref ref="ASYNC_FILE"/>
        </root>
    </springProfile>

    <springProfile name="test">
        <logger name="org.openfinance" level="ERROR"/>
        <!-- Silence noisy AOP aspects that emit a stack trace per expected validation failure -->
        <logger name="org.openfinance.util.LoggingAspect" level="OFF"/>
        <logger name="org.openfinance.config.PerformanceMonitoringAspect" level="OFF"/>
        <logger name="org.springframework" level="WARN"/>
        <logger name="org.hibernate" level="WARN"/>
        <logger name="com.zaxxer.hikari" level="WARN"/>
        <logger name="org.flywaydb" level="WARN"/>
        <logger name="io.jsonwebtoken" level="WARN"/>
        <root level="WARN">
            <appender-ref ref="CONSOLE_TEST"/>
        </root>
    </springProfile>

    <springProfile name="prod">
        <!-- Production: INFO only, no console output, structured log to file -->
        <logger name="org.openfinance" level="INFO"/>
        <logger name="org.hibernate.SQL" level="ERROR"/>
        <logger name="org.springframework.web" level="WARN"/>
        <root level="INFO">
            <appender-ref ref="ASYNC_FILE"/>
            <appender-ref ref="ASYNC_ERROR_FILE"/>
        </root>
    </springProfile>

</configuration>
