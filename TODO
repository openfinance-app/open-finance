TODO:

5-There are still some amount display that are not wrapped with ConvertedAmount component, ex: net cash flow amount in the cash flow card.

That is not the only place where the amount is not wrapped with ConvertedAmount component.

As a reminder, all amount display should be wrapped with ConvertedAmount component to ensure proper currency conversion and formatting.

Review the codebase and identify all instances where amounts are displayed, and ensure that they are wrapped with the ConvertedAmount component.

- Subscription Plan Implementation:
    - Landing page
        - Design sure
        - Capture screenshot
        - Live demo
    - Waiting list
        - Email capture
    - Stripe
        - 1 month trial
            - Custom Features available only via subscription
                - account syncing: Lunch Flow.
                - More accurate Financial data (rates, quotes) with external provider.
                - AI services: OpenAI.
                - Email notif
